//class that contains constructors for all the shapes that extend from this class
class shapes {
    constructor(logoColor, text, textColor) {
        this.logoColor = logoColor;
        this.text = text;    
        this.textColor = textColor;
    }
};

module.exports = shapes 
